@page
@model StudentTopicWeb.Pages.Disciplines.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Discipline[0].DiscName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discipline[0].UserId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Discipline) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DiscName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DisciplineId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DisciplineId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DisciplineId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
