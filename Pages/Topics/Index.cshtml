@page
@model StudentTopicWeb.Pages.Topics.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Topic[0].WorkId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Topic[0].TopicName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Topic[0].Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Topic) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WorkId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TopicName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TopicId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TopicId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TopicId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
